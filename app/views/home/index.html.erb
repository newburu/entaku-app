<h1>Welcome to the Seating Chart App</h1>

<% if logged_in? %>
  <p>Hello, <%= Current.user.name %>!</p>
  <%= button_to "Logout", logout_path, method: :delete %>
<% else %>
  <%= link_to "Sign Up", sign_up_path %>
  <%= link_to "Login", login_path %>
<% end %>
